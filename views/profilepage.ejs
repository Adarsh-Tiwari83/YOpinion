<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User</title>
</head>
<body>
    <h1>Kaal ki kaynaat me welcome , <%= user.name %></h1>
    <a href="/logout">Logout </a>
    <a href="/<%= user._id %>/newBlog" target="_blank" rel="noopener noreferrer">Create Blog</a>
    <% user.opinions.forEach(element => { %>
    <h1> <%= element.title %> </h1>
    <p> <%= element.content %> </p>
    <form action="/like/<%= user._id %>" method="post">
        <button type="submit">Likes : <%= element.likes %></button>
    </form>
    <form action="/dislike/<%= user._id %>" method="post">
        <button type="submit">Dislikes : <%= element.dislikes %></button>
    </form>
    <br>
    <% }); %>
    
</body>
</html>